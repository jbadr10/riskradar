# I used yfinance which does not require an API key. 
#If you want to use a different data source that requires an API key,
# you can add it to a .env file and access it in your code using the os module.